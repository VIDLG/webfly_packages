name: webfly_ble
description: Flutter BLE wrapper around flutter_blue_plus for reuse across projects.
version: 0.1.0

publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.1.0
  webf: ^0.24.11
  webfly_bridge:
    path: ../webfly_bridge
  anyhow: ^3.0.1
  json_annotation: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  json_serializable: ^6.12.0
