name: webfly_updater
description: Reusable update checker and OTA installer (GitHub Releases + ota_update).
version: 0.1.0

publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^9.0.0
  dio: ^5.4.0
  logging: ^1.3.0
  pub_semver: ^2.1.5
  open_filex: ^4.7.0
  path_provider: ^2.1.5

